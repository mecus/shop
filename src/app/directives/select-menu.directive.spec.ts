import { SelectMenuDirective } from './select-menu.directive';

describe('SelectMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectMenuDirective();
    expect(directive).toBeTruthy();
  });
});
