import { AdvertDirective } from './advert.directive';

describe('AdvertDirective', () => {
  it('should create an instance', () => {
    const directive = new AdvertDirective();
    expect(directive).toBeTruthy();
  });
});
